# Config file for running as Home Assistent Add-on
name: "wuFlow"
version: "0.9.0"
slug: "wuflow"
description: "Simple issue tracking app"
arch:
  - aarch64
  - amd64
startup: application
boot: auto
map:
  - type: share
    read_only: False
ports:
  8080/tcp: 5555
options: {}
schema: {}
